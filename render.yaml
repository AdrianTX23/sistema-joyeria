services:
  - type: web
    name: sistema-joyeria
    env: node
    plan: free
    buildCommand: npm run install-all
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
