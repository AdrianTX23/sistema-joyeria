{
  "name": "jewelry-inventory-system",
  "version": "1.0.0",
  "description": "Sistema de inventario y ventas para joyería con diseño minimalista",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "npm run install-all && cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "node build-config.js && npm run install-all && cd client && npm run build",
    "start:prod": "cd server && npm start",
    "heroku-postbuild": "npm run install-all",
    "start": "cd server && npm start"
  },
  "keywords": ["inventory", "sales", "jewelry", "react", "nodejs"],
  "author": "Jewelry System",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
